import { latLng, latLngBounds } from 'leaflet';

export const DEFAULT_BOUNDS = latLngBounds(latLng(-47.3, 166.3), latLng(-40.5, 174.6));
export const DEFAULT_ZOOM = 7;
export const SELECT_ZOOM = 12;
export const SURVEY_ZOOM = 11.5;
